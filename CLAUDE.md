## Agent skills

### Backlog

Issues live in GitHub Issues (`WireCell/wire-cell-bee3`). See `docs/agents/backlog.md`.

### Triage labels

Default canonical labels with one override: `enhancement` is used in place of `needs-triage`. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
